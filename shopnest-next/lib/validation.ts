import { z } from 'zod';
export const productSchema = z.object({ name: z.string().min(1), brand: z.string().min(1), description: z.string().min(1), price: z.coerce.number().int().nonnegative(), originalPrice: z.coerce.number().int().nonnegative().optional(), image: z.string().url(), images: z.array(z.string().url()).default([]), category: z.string().min(1), stock: z.coerce.number().int().nonnegative().default(0), rating: z.coerce.number().min(0).max(5).default(0), discount: z.coerce.number().int().min(0).max(100).default(0), tags: z.array(z.string()).default([]), isFeatured: z.boolean().default(false) });
export const addressSchema = z.object({ label: z.string().default('Home'), doorNo: z.string().min(1), street: z.string().min(1), city: z.string().min(1), district: z.string().min(1), state: z.string().min(1), pincode: z.string().min(4), isDefault: z.boolean().optional() });
export const profileSchema = z.object({ name: z.string().min(1).optional(), phone: z.string().regex(/^\d{10}$/).optional(), age: z.coerce.number().int().positive().optional(), address: addressSchema.optional() });
